{
  "name": "pikopark-online",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "lint": "eslint \"client/src/**/*.ts\" \"server/src/**/*.ts\" \"shared/**/*.ts\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
