# Copy to .env.local and fill in your values.
# During local dev this defaults to ws://localhost:2567 — no change needed.
# For production set this to your Render server URL (wss://).

VITE_SERVER_URL=wss://pikopark-server.onrender.com
